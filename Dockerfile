# Используем официальный образ Python версии 3.11 на базе slim (легковесный вариант)
FROM python:3.11-slim

# Устанавливаем рабочую директорию внутри контейнера как /app
WORKDIR /app

# Копируем файл с зависимостями requirements.txt в рабочую директорию контейнера
COPY requirements.txt .

# Устанавливаем зависимости из requirements.txt без кеширования для уменьшения размера образа
RUN pip install --no-cache-dir -r requirements.txt

# Копируем основной файл приложения main.py в рабочую директорию контейнера
COPY main.py .

# Открываем порт 8000 для внешнего доступа к приложению
EXPOSE 8000

# Команда для запуска приложения: uvicorn сервер на всех интерфейсах, порт 8000
CMD ["uvicorn", "main:app", "--host", "0.0.0.0", "--port", "8000"]
